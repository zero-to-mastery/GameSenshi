const CARD = 'card'
const CARD_CARDS = 'card_cards'
const CARD_IS = CARD + 'Is'
const CARD_TYPE = CARD + 'Type'
const CARD_EXPIRY = CARD + 'Expiry'
const CARD_NUMBER = CARD + 'Number'
const CARD_IS_DEFAULT = CARD_IS + 'Default'
const CARD_EXPIRY_MONTH = CARD_EXPIRY + 'Month'
const CARD_EXPIRY_YEAR = CARD_EXPIRY + 'Year'
const CARD_LAST_4_DIGITS = CARD + 'Last4Digits'

export {
	CARD_CARDS,
	CARD_TYPE,
	CARD_NUMBER,
	CARD_IS_DEFAULT,
	CARD_EXPIRY_MONTH,
	CARD_EXPIRY_YEAR,
	CARD_LAST_4_DIGITS,
}
